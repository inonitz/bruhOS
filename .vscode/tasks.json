{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMake Build",
            "type": "shell",
            "command": "./build.sh debug build",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}